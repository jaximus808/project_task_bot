import { COMMAND_TYPE, OPERATION_TYPE } from "../../utils/consts";


// TODO: Do this, For now just have this 
export const ProjectAuth = (user_id: string, operation: OPERATION_TYPE) : boolean => {
    return true;
}